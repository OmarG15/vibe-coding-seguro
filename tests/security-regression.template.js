const test = require('node:test');
const assert = require('node:assert/strict');

// Plantilla para el participante.
// Convierte la evidencia manual del laboratorio en una prueba automatizada.
// La solución completa se deja intencionalmente sin implementar.

test.skip('un usuario no puede leer evidencia de otro usuario', async () => {
  // 1. Inicia la aplicación o usa la instancia local del laboratorio.
  // 2. Inicia sesión como Alice.
  // 3. Solicita una evidencia que pertenezca a Bob.
  // 4. Define el status esperado después del fix (403 o 404 según tu diseño).
  // assert.equal(response.status, 403);
  assert.ok(true);
});
